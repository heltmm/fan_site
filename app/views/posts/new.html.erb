<h1>New Post</h1>

<%= form_for [@post] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :content %><br>
  <%= f.text_area :content %><br>
  <%= f.hidden_field(:user_id, :value => current_user.id) %>

  <%= f.submit %>
<% end %>
